---
- name: Install remote-boot-agent systemd shutdown service
  ansible.builtin.template:
    src: remote-boot-agent.service.j2
    dest: /etc/systemd/system/remote-boot-agent.service
    owner: root
    group: root
    mode: '0644'
  notify: Enable systemd service
